// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: set to your real domain — used for canonical and OG URLs.
  site: 'https://example.com',
});
